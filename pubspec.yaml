name: dari_version_complete
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=3.5.4 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.0
  table_calendar: ^3.0.8
  cupertino_icons: ^1.0.8
  font_awesome_flutter: ^10.0.0
  image_picker: ^0.8.7+4
  shared_preferences: ^2.0.17

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/
